<?xml version="1.0" encoding="utf-8"?>
<!-- Shop LAN uses HTTP (no TLS on WEBSTUDIO Server until explicitly configured). -->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
