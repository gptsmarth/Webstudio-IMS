# Fonts Directory

This folder contains locally bundled variable TrueType font files guaranteeing 100% offline typography.

## Bundled Fonts
- `Inter-Variable.ttf` — Primary UI Sans typeface
- `JetBrainsMono-Variable.ttf` — Monospace numbers and code font

Configured via `@font-face` rules in `src/fonts.css`.
