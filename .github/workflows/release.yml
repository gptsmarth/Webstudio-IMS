# WEBSTUDIO IMS — release
# Workflow to be configured when development begins
name: release
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Workflow placeholder'
