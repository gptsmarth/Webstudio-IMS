# WEBSTUDIO IMS — ci-database
# Workflow to be configured when development begins
name: ci-database
on:
  workflow_dispatch:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Workflow placeholder'
